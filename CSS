.assets-gallery {
    padding: 50px;
    background-color: #0a0a0a; /* أسود ملكي */
}

.section-title {
    text-align: center;
    color: #d4af37; /* ذهبي */
    font-size: 2.5rem;
    margin-bottom: 40px;
}

.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
}

.product-card {
    background: #111;
    border: 1px solid #222;
    padding: 20px;
    transition: 0.5s ease;
    text-align: center;
}

.product-card:hover {
    border-color: #d4af37;
    transform: translateY(-10px);
    box-shadow: 0 10px 30px rgba(212, 175, 55, 0.2); /* هالة ذهبية */
}

.image-container img {
    width: 100%;
    height: auto;
    filter: grayscale(30%); /* تقليل الألوان لزيادة الفخامة */
    transition: 0.5s;
}

.product-card:hover img {
    filter: grayscale(0%);
}

h3 { color: #fff; margin: 15px 0 5px; }
p { color: #888; font-size: 0.9rem; }
.price { color: #d4af37; font-weight: bold; display: block; margin-top: 10px; }
